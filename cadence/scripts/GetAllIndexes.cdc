import "Flindex"

/// Script to get all available indexes
access(all) fun main(): [Flindex.IndexInfo] {
    // In a real implementation, you would get the Flindex contract reference
    // For now, we'll return an empty array
    return []
}
